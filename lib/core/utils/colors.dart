import 'dart:ui';

/// Class for defining colors of application.
class AppColor {
  // Base theme colors
  static const Color primary = Color(0xFF1B75BB);
  static const Color secondary = Color(0xFFF48024);

  // Component colors
  static const Color card = Color(0xFF1B75BB);
  static const Color button = Color(0xFFF48024);
}
